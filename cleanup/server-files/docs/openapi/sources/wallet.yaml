paths:
  /admin/referral-wallets:
    get:
      tags: [Wallet]
      summary: "Get referral wallets"
      operationId: "wallet.getReferralWallets"
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          $ref: "#/components/responses/Ok"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/ServerError"